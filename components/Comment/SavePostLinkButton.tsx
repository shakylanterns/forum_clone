import React from "react";
import LinkButton from "../Base/LinkButton";

type Props = {};

const SavePostLinkButton = (props: Props) => {
  const savePost = () => {};
  return <LinkButton action={savePost}>Save Card</LinkButton>;
};

export default SavePostLinkButton;
